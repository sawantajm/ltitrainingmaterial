import { RegDirective } from './reg.directive';

describe('RegDirective', () => {
  it('should create an instance', () => {
    const directive = new RegDirective();
    expect(directive).toBeTruthy();
  });
});
