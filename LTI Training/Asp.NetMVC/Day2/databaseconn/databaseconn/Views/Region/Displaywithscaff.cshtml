@model IEnumerable<databaseconn.Models.Region>

@{
    ViewData["Title"] = "Displaywithscaff";
}

<h1>Displaywithscaff</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RegionDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RegionDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RegionId">Edit</a> |
                <a asp-action="DisplayPerticularData" asp-route-id="@item.RegionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RegionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
